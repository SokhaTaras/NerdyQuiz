export enum STORAGE_ERROR_MESSAGE {
  PARSE = 'Error parsing JSON from localStorage',
  STRINGIFY = 'Error stringify JSON from localStorage',
  SET_DATA = 'Error while set to localStorage',
  GET_DATA = 'Error while get from localStorage'
}
